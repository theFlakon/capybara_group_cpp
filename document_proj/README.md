# Document Batch Processing System

A C++ educational project demonstrating runtime polymorphism via a document processing pipeline.
Supports TXT, PDF, and CSV formats through a common abstract `Document` interface.

## Build & Run

```bash
cmake -S . -B build
cmake --build build
./build/bin/program
```